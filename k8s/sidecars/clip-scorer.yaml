# ExternalName service for the native CLIP scorer FastAPI sidecar.
apiVersion: v1
kind: Service
metadata:
  name: clip-scorer
  namespace: pipeline
spec:
  type: ExternalName
  externalName: host.lima.internal
  ports:
    - port: 12002
      targetPort: 12002
