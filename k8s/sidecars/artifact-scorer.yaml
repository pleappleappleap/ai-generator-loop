# ExternalName service for the native artifact scorer FastAPI sidecar.
apiVersion: v1
kind: Service
metadata:
  name: artifact-scorer
  namespace: pipeline
spec:
  type: ExternalName
  externalName: host.lima.internal
  ports:
    - port: 12003
      targetPort: 12003
