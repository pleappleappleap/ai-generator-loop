# ExternalName service for the native VLM scorer FastAPI sidecar.
apiVersion: v1
kind: Service
metadata:
  name: vlm-scorer
  namespace: pipeline
spec:
  type: ExternalName
  externalName: host.lima.internal
  ports:
    - port: 12004
      targetPort: 12004
