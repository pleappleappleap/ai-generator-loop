\documentclass[11pt,letterpaper]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{newtxtext,newtxmath}
\usepackage{microtype}
\usepackage[margin=1.25in]{geometry}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,backgrounds,calc}
\usepackage{booktabs}
\usepackage{listings}
\usepackage{xcolor}
\usepackage[colorlinks=true,linkcolor=black,urlcolor=black,citecolor=black]{hyperref}

\lstdefinestyle{shell}{
  basicstyle=\small\ttfamily,
  backgroundcolor=\color{gray!8},
  frame=single,
  framerule=0.4pt,
  breaklines=true,
  breakatwhitespace=false,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
}

\lstdefinestyle{xml}{
  basicstyle=\small\ttfamily,
  backgroundcolor=\color{gray!8},
  frame=single,
  framerule=0.4pt,
  breaklines=true,
  language=XML,
  showstringspaces=false,
}

\lstdefinestyle{yaml}{
  basicstyle=\small\ttfamily,
  backgroundcolor=\color{gray!8},
  frame=single,
  framerule=0.4pt,
  breaklines=true,
  showstringspaces=false,
}

\title{\textbf{AI Image Generation Pipeline}\\[0.5em]\large Technical Manual}
\author{Sridhar Ayengar}
\date{\today}

\tikzset{
  component/.style={rectangle,rounded corners=3pt,draw=black,thick,fill=gray!10,
    minimum width=2.4cm,minimum height=0.7cm,font=\small\ttfamily,text centered},
  queue/.style={rectangle,draw=black,thick,fill=white,
    minimum width=2.4cm,minimum height=0.6cm,font=\small\ttfamily,text centered},
  exchange/.style={diamond,draw=black,thick,fill=gray!20,
    minimum width=2.2cm,minimum height=0.7cm,font=\small\ttfamily,text centered,aspect=2.5},
  store/.style={cylinder,draw=black,thick,fill=gray!15,shape border rotate=90,
    minimum width=1.8cm,minimum height=0.8cm,font=\small\ttfamily,text centered},
  arrow/.style={->,>=Stealth,thick},
  lbl/.style={font=\footnotesize\ttfamily,midway}
}

\begin{document}

\maketitle
\tableofcontents
\newpage

% ============================================================
\section{Overview}
% ============================================================

The AI image generation pipeline is an autonomous system that accepts a
natural language prompt and iteratively generates, scores, and refines
images until a candidate meeting all quality thresholds is accepted by a
tactical large language model (LLM).

A browser-based pipeline UI at \texttt{http://localhost:7860} provides the
primary operator interface: create and resume creative workflows, watch images
arrive in a live gallery as they are generated and scored, send thumbs
up/down feedback, and chat with the tactical LLM to refine creative direction.
The UI backend is a FastAPI server that subscribes to Artemis topics and
streams events to the browser over WebSocket.

The pipeline is designed around three principles:

\begin{enumerate}
  \item \textbf{Observable}: every message, score, and verdict flows through
        inspectable queues and addresses. Artemis's management console
        provides real-time visibility into all message flows. Dead-letter
        messages accumulate in \texttt{pipeline.dead} for operator review.
        The browser UI streams image and decision events live.
  \item \textbf{Scalable}: each component is independently deployable. The
        message broker provides natural decoupling. Adding scorer capacity
        requires only adding subscriber processes.
  \item \textbf{Persistent}: every generated image is recorded in LanceDB as
        a vector embedding alongside full scorer results and generation
        parameters. This long-term memory enables the strategic LLM to
        identify patterns across sessions.
\end{enumerate}

% ============================================================
\section{Installation}
% ============================================================

\subsection{Hardware Requirements}

The pipeline supports three GPU backends. Choose the row that matches your
hardware:

\begin{center}
\begin{tabular}{llll}
\toprule
\textbf{Platform} & \textbf{Requirement} & \textbf{Minimum} & \textbf{Recommended} \\
\midrule
Apple Silicon (MPS) & Chip            & M1 / M2 / M3       & M3 Ultra \\
                    & Unified memory  & 64\,GB             & 96\,GB \\
\midrule
NVIDIA (CUDA)       & GPU VRAM        & 24\,GB             & 48\,GB+ \\
                    & System RAM      & 32\,GB             & 64\,GB \\
\midrule
AMD (ROCm)          & GPU VRAM        & 24\,GB             & 48\,GB+ \\
                    & System RAM      & 32\,GB             & 64\,GB \\
\midrule
CPU only            & System RAM      & 64\,GB             & 128\,GB+ \\
                    & \multicolumn{3}{l}{\textit{Functional but very slow. Expect hours per image, not minutes.}} \\
\midrule
All platforms       & Free disk       & 100\,GB            & 200\,GB \\
\bottomrule
\end{tabular}
\end{center}

The pipeline holds approximately 44\,GB of model weights in GPU memory
simultaneously (ComfyUI + SDXL $\approx$14\,GB; tactical LLM $\approx$43\,GB;
scorers $\approx$8\,GB). On discrete-GPU systems the tactical LLM can be
offloaded partially to CPU via the \texttt{n\_gpu\_layers} config key.
See Section~\ref{sec:memory} for the per-component breakdown.

\subsection{Bootstrap Prerequisites}

The following must be in place \emph{before} running \texttt{make}.
Everything else is installed automatically by \texttt{make prereqs-system}.

\subsubsection{macOS}

\begin{lstlisting}[style=shell]
# Xcode Command Line Tools (git, make, clang, curl)
xcode-select --install

# Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
\end{lstlisting}

Follow the post-install instructions to add Homebrew to your PATH.

\subsubsection{Linux}

Most distributions include git and make. If not:

\begin{lstlisting}[style=shell]
# Debian / Ubuntu
sudo apt install git make

# Fedora / RHEL
sudo dnf install git make
\end{lstlisting}

\subsubsection{Windows}

\begin{enumerate}
  \item \textbf{Git Bash} --- \url{https://git-scm.com/downloads}. Provides
    bash, git, make, curl, and wget. Run all \texttt{make} commands from Git
    Bash, not CMD or PowerShell.
  \item \textbf{winget} --- ships with Windows~11. On Windows~10, install the
    App Installer from the Microsoft Store.
\end{enumerate}

\subsubsection{Clone the repository}

\begin{lstlisting}[style=shell]
git clone <repo-url> ~/ai-image
cd ~/ai-image
export AI_IMAGE_ROOT=~/ai-image   # add to shell profile
\end{lstlisting}

\subsection{System Prerequisites (\texttt{make prereqs-system})}

With the bootstrap in place, run \texttt{make prereqs-system} to install the
remaining tools automatically. The table below shows what is installed per
platform.

\begin{center}
\begin{tabular}{llll}
\toprule
\textbf{Tool} & \textbf{macOS} & \textbf{Linux} & \textbf{Notes} \\
\midrule
wget        & ---  & \checkmark & macOS uses curl (system native) \\
Python 3.11 & \checkmark & \checkmark & via Homebrew / distro packages \\
yq          & \checkmark & \checkmark & YAML query tool for shell scripts \\
Docker      & \checkmark & \checkmark & Desktop (macOS/Windows) or Engine (Linux) \\
Rust/cargo  & \checkmark & \checkmark & via rustup \\
sqlx-cli    & \checkmark & \checkmark & SQLite compile-time query verification \\
protoc      & \checkmark & \checkmark & required by LanceDB Rust crate \\
\bottomrule
\end{tabular}
\end{center}

\LaTeX{} is \emph{not} installed automatically --- it is only needed to
regenerate this document (\texttt{make doc}).

\subsection{ActiveMQ Artemis (Docker)}

The pipeline runs Artemis via the official Docker image
(\texttt{apache/activemq-artemis}). AMQP~1.0 (port 5672), STOMP (port
61613), and the management console (port 8161) are pre-enabled in the image
--- no \texttt{broker.xml} editing is required.

\texttt{make prereqs-system} installs Docker; \texttt{make all} pulls the
image. To pull the image on its own:

\begin{lstlisting}[style=shell]
make artemis-broker
\end{lstlisting}

Start the broker:

\begin{lstlisting}[style=shell]
loop/start_broker.sh
\end{lstlisting}

Broker data is persisted in \texttt{loop/artemis-data/} (bind-mounted into
the container). The management console is available at
\texttt{http://localhost:8161} (credentials: \texttt{admin} / \texttt{admin}).

\subsection{Python Environments}

There are three separate virtual environments:

\begin{itemize}
  \item \textbf{Root venv} (\texttt{venv/}): used by
        \texttt{comfyui\_worker.py}, \texttt{monitor.py}, and utilities.
  \item \textbf{Scorers venv} (\texttt{loop/scorers/venv/}): used by the
        three scorer processes, \texttt{tactical\_llm.py}, and the
        \textbf{UI server} (\texttt{loop/ui/server.py}). These processes
        share large ML libraries (transformers, llama-cpp-python) and the
        UI web framework (fastapi, uvicorn, httpx).
  \item \textbf{ComfyUI venv} (\texttt{loop/ComfyUI/venv/}): used
        exclusively by ComfyUI and managed by \texttt{make prereqs}.
\end{itemize}

\subsubsection{Root environment}

\begin{lstlisting}[style=shell]
cd $AI_IMAGE_ROOT
python3.11 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
deactivate
\end{lstlisting}

\subsubsection{Scorers environment}

Install \texttt{llama-cpp-python} with the correct GPU backend flag for your
platform before installing the remaining requirements:

\begin{center}
\begin{tabular}{ll}
\toprule
\textbf{Platform} & \textbf{CMAKE\_ARGS} \\
\midrule
macOS (Metal / MPS)  & \texttt{CMAKE\_ARGS="-DLLAMA\_METAL=on"} \\
NVIDIA (CUDA)        & \texttt{CMAKE\_ARGS="-DGGML\_CUDA=on"} \\
AMD (ROCm / HIP)     & \texttt{CMAKE\_ARGS="-DGGML\_HIPBLAS=on"} \\
CPU only             & (no flag required) \\
\bottomrule
\end{tabular}
\end{center}

\begin{lstlisting}[style=shell]
cd $AI_IMAGE_ROOT/loop/scorers
python3.11 -m venv venv
source venv/bin/activate
pip install --upgrade pip

# Set CMAKE_ARGS per the table above, then:
pip install llama-cpp-python --no-binary llama-cpp-python

pip install -r requirements.txt
deactivate
\end{lstlisting}

\texttt{make prereqs-python} runs all of the above automatically, including
GPU backend detection.

\subsection{Model Downloads}

\subsubsection{SDXL checkpoint (ComfyUI)}

Download a Stable Diffusion XL checkpoint (e.g., Absolute Reality XL) and
place it in:

\begin{lstlisting}[style=shell]
loop/ComfyUI/models/checkpoints/
\end{lstlisting}

Workflow JSON files under \texttt{loop/workflows/} reference the checkpoint
by filename. Update \texttt{config.yaml} if the filename differs from the
workflow default.

\subsubsection{AI artifact detector}

\begin{lstlisting}[style=shell]
cd $AI_IMAGE_ROOT/loop/scorers
source venv/bin/activate
python - <<'EOF'
from huggingface_hub import snapshot_download
snapshot_download(
    repo_id="umm-maybe/AI-image-detector",
    local_dir="models/artifact-detector"
)
EOF
\end{lstlisting}

\subsubsection{VLM scorer (Qwen2.5-VL-7B)}

\begin{lstlisting}[style=shell]
mkdir -p $AI_IMAGE_ROOT/loop/scorers/models/vlm
curl -L -o $AI_IMAGE_ROOT/loop/scorers/models/vlm/Qwen2.5-VL-7B-Instruct-Q5_K_M.gguf \
  "https://huggingface.co/bartowski/Qwen2.5-VL-7B-Instruct-GGUF/resolve/main/\
Qwen2.5-VL-7B-Instruct-Q5_K_M.gguf"
\end{lstlisting}

Approximately 5\,GB download.

\subsubsection{Tactical LLM (Qwen3-72B)}

Qwen3-72B is a hybrid thinking model: it can reason step-by-step through
ambiguous decisions (retry vs.\ inpaint, prompt diagnosis) and respond
directly for clear-cut cases, controlled by a \texttt{<think>} token in
the prompt.

\begin{lstlisting}[style=shell]
mkdir -p $AI_IMAGE_ROOT/loop/scorers/models/tactical
curl -L -o $AI_IMAGE_ROOT/loop/scorers/models/tactical/Qwen3-72B-abliterated-Q4_K_M.gguf \
  "https://huggingface.co/bartowski/Qwen3-72B-abliterated-GGUF/resolve/main/\
Qwen3-72B-abliterated-Q4_K_M.gguf"
\end{lstlisting}

Approximately 43\,GB download. Verify the filenames match
\texttt{config.yaml.default} keys \texttt{models.vlm.filename} and
\texttt{tactical.model.filename}, or update \texttt{config.yaml} to match
what you downloaded.

\subsection{ComfyUI}

\texttt{make prereqs} clones ComfyUI into \texttt{loop/ComfyUI/} and
installs its dependencies into a dedicated venv at
\texttt{loop/ComfyUI/venv/}. To run this step manually:

\begin{lstlisting}[style=shell]
git clone --depth=1 https://github.com/comfyanonymous/ComfyUI /tmp/_comfyui_clone
cp -rn /tmp/_comfyui_clone/. $AI_IMAGE_ROOT/loop/ComfyUI/
rm -rf /tmp/_comfyui_clone
python3.11 -m venv $AI_IMAGE_ROOT/loop/ComfyUI/venv
$AI_IMAGE_ROOT/loop/ComfyUI/venv/bin/pip install -r $AI_IMAGE_ROOT/loop/ComfyUI/requirements.txt
\end{lstlisting}

\subsubsection{Custom nodes}

The inpainting workflow requires ComfyUI-Manager and additional custom nodes.
Start ComfyUI, open \texttt{http://127.0.0.1:8188}, install
\textbf{ComfyUI-Manager} from the Manager menu, then use Manager to install:

\begin{itemize}
  \item \texttt{ComfyUI-Inpaint-Nodes}
  \item \texttt{comfyui\_segment\_anything}
\end{itemize}

\subsubsection{Optional vision models (inpainting)}

\begin{lstlisting}[style=shell]
# Segment Anything (SAM)
mkdir -p loop/ComfyUI/models/sam
curl -L -o loop/ComfyUI/models/sam/sam_vit_h_4b8939.pth \
  "https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth"

# Grounding DINO
mkdir -p loop/ComfyUI/models/grounding-dino
curl -L -o loop/ComfyUI/models/grounding-dino/groundingdino_swint_ogc.pth \
  "https://github.com/IDEA-Research/GroundingDINO/releases/download/\
v0.1.0-alpha/groundingdino_swint_ogc.pth"
\end{lstlisting}

\subsection{Configuration}

Copy the default template and open the interactive editor:

\begin{lstlisting}[style=shell]
cd $AI_IMAGE_ROOT
cp config.yaml.default config.yaml
python menuconfig.py
\end{lstlisting}

At minimum, verify these keys in \texttt{config.yaml}:

\begin{lstlisting}[style=yaml]
compute:
  comfyui:
    backend: auto      # auto | mps | cuda | rocm | cpu
    listen: 0.0.0.0    # bind address; 127.0.0.1 to restrict to localhost
    device_id:         # optional: integer index for eGPU or multi-GPU systems

broker:
  amqp_url:     amqp://admin:admin@localhost:5672    # Rust components
  stomp_url:    stomp://admin:admin@localhost:61613  # Python components
  artemis_data: auto   # resolves to {repo_root}/loop/artemis-broker

database:
  path: auto           # resolves to {repo_root}/pipeline.db

models:
  vlm:
    filename: Qwen2.5-VL-7B-Instruct-Q5_K_M.gguf

tactical:
  model:
    filename:   Qwen3-72B-abliterated-Q4_K_M.gguf
    server_url: http://localhost:8080/v1   # llama.cpp OpenAI-compatible server
    model_name: default                   # model name sent in API requests

ui:
  port: 7860       # browser UI port
  host: 0.0.0.0    # bind address; 127.0.0.1 to restrict to localhost
  # chat_backend:  # optional override for the chat LLM
  #   server_url: http://localhost:8081/v1
  #   model_name: strategic-model
\end{lstlisting}

The \texttt{backend: auto} setting detects the platform at startup:
macOS~$\to$~MPS; NVIDIA present~$\to$~CUDA; AMD ROCm present~$\to$~ROCm;
otherwise CPU. Set \texttt{device\_id} to an integer device index when
multiple GPUs are present (e.g.\ an eGPU over Thunderbolt).

Run the environment checker to verify all dependencies are resolvable before
starting the pipeline:

\begin{lstlisting}[style=shell]
python check_env.py
\end{lstlisting}

\subsection{Building the Rust Binaries}

\begin{lstlisting}[style=shell]
cd $AI_IMAGE_ROOT/loop/scorers
cargo build --release
\end{lstlisting}

This produces four binaries in \texttt{target/release/}:

\begin{center}
\begin{tabular}{ll}
\toprule
\textbf{Binary} & \textbf{Role} \\
\midrule
\texttt{router}          & Fans out \texttt{loop.events} $\to$ \texttt{scorer.requests} \\
\texttt{aggregator}      & Merges scorer results; applies threshold logic; emits verdicts \\
\texttt{coordinator}     & XA 2PC log; workflow/conversation hierarchy; Python API (Unix socket) \\
\texttt{lancedb\_manager} & Writes terminal Loop records to LanceDB \\
\bottomrule
\end{tabular}
\end{center}

% ============================================================
\section{Technology Stack}
% ============================================================

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Layer} & \textbf{Technology} & \textbf{Rationale} \\
\midrule
Message broker & ActiveMQ Artemis (AMQP~1.0 / STOMP) & Native AMQP~1.0; STOMP for Python clients \\
Operational state & SQLite WAL (\texttt{pipeline.db}) & Crash-safe, zero-dependency, single machine \\
Vector store & LanceDB & Embedded, Arrow-native, supports ANN search \\
Image generation & ComfyUI + SDXL & Workflow-based; supports ControlNet / LoRA \\
Holistic scoring & Qwen2.5-VL-7B (Q5\_K\_M) & Fast local VLM; no API dependency \\
Tactical LLM & Qwen3-72B (Q4\_K\_M, hybrid thinking) via llama.cpp & Local; OpenAI-compatible server \\
Browser UI & FastAPI + uvicorn + WebSocket & Workflow management; live gallery; chat \\
Rust binaries & router, aggregator, coordinator, lancedb\_manager & Zero-GIL concurrency; compile-time safety \\
Python processes & scorers, comfyui\_worker, tactical\_llm, ui/server & ML model hosting; stomp.py for STOMP \\
\bottomrule
\end{tabular}
\end{center}

% ============================================================
\section{Component Architecture}
% ============================================================

\subsection{Language Selection}

Four components are implemented in Rust:

\begin{description}
  \item[Router] Zero business logic, no ML dependencies. A compiled binary
        with negligible memory footprint ($\sim$5\,MB) is appropriate for
        pure message routing.
  \item[Aggregator] Manages concurrent state across multiple incoming scorer
        result streams. Rust's ownership model prevents data races at compile
        time. State is accumulated in SQLite via \texttt{BEGIN IMMEDIATE}.
  \item[Coordinator] XA 2-phase-commit manager and Python API. Owns all
        SQLite tables: \texttt{xa\_log}, \texttt{scorer\_session},
        \texttt{tactical\_budget}, \texttt{conversation}, \texttt{workflow},
        \texttt{feedback}, and \texttt{chat\_message}. Exposes a Unix domain
        socket for synchronous JSON RPC from Python processes.
  \item[LanceDB Manager] Terminal event writer. Reads accumulated scorer
        results from SQLite and writes a single durable Loop record to LanceDB
        on each \texttt{loop.accepted} or \texttt{cancel.*} event.
\end{description}

All components with ML model dependencies are Python (STOMP via stomp.py).
The \textbf{llama.cpp server} runs as a standalone process (started by
\texttt{start\_loop.sh}) and exposes an OpenAI-compatible HTTP API on port
8080. Both \texttt{tactical\_llm.py} and the UI chat endpoint consume it via
the \texttt{openai} Python client, so the model is loaded exactly once.

\subsection{Component Diagram}

\begin{center}
\begin{tikzpicture}[node distance=0.9cm and 1.4cm]
  % Generation layer
  \node[component] (worker)    {comfyui\_worker};
  \node[component,below=0.5cm of worker] (comfyui) {ComfyUI};
  % Routing
  \node[component,right=1.6cm of worker]  (router)   {router (Rust)};
  % Scorers
  \node[component,right=1.6cm of router,yshift=1.4cm]  (clip)     {clip\_scorer};
  \node[component,right=1.6cm of router]               (artifact) {artifact\_scorer};
  \node[component,right=1.6cm of router,yshift=-1.4cm] (vlm)      {vlm\_scorer};
  % Aggregator and downstream
  \node[component,right=1.6cm of artifact] (agg)      {aggregator};
  \node[component,right=1.6cm of agg,yshift=1.0cm]  (tactical) {tactical LLM};
  \node[component,right=1.6cm of agg,yshift=-1.0cm] (ldb)      {lancedb\_mgr};
  % Stores
  \node[store,below=1.2cm of agg]  (sqlite)  {pipeline.db};
  \node[store,right=0.4cm of sqlite] (lancedb) {LanceDB};
  % Coordinator
  \node[component,below=0.6cm of sqlite] (coord) {coordinator};
  % LLM server and UI
  \node[component,above=0.8cm of tactical] (llmserver) {llama.cpp server};
  \node[component,above=0.8cm of worker]   (ui)        {UI server};

  % Generation path
  \draw[arrow] (worker)   -- (router)   node[lbl,above] {loop.events};
  \draw[arrow] (router)   -- (clip)     node[lbl,above,xshift=-3mm] {scorer.requests};
  \draw[arrow] (router)   -- (artifact);
  \draw[arrow] (router)   -- (vlm);
  \draw[arrow] (clip)     -- (agg);
  \draw[arrow] (artifact) -- (agg);
  \draw[arrow] (vlm)      -- (agg);
  \draw[arrow] (agg)      -- (tactical) node[lbl,above] {scorer.result};
  \draw[arrow] (agg)      -- (ldb)      node[lbl,right] {cancel.*};
  \draw[arrow] (tactical) -- (ldb)      node[lbl,right,xshift=2mm] {loop.accepted};
  \draw[arrow] (ldb)      -- (lancedb);
  \draw[arrow] (agg)      -- (sqlite);
  \draw[arrow] (worker)   -- (comfyui)  node[lbl,right] {HTTP/WS};
  % Socket connections
  \draw[arrow,dashed] (worker)   -- (coord)     node[lbl,right,yshift=-4mm] {socket};
  \draw[arrow,dashed] (tactical) -- (coord)     node[lbl,right] {socket};
  \draw[arrow,dashed] (ui)       -- (coord)     node[lbl,left]  {socket};
  % LLM server
  \draw[arrow,dashed] (tactical) -- (llmserver) node[lbl,right] {HTTP};
  \draw[arrow,dashed] (ui)       -- (llmserver) node[lbl,above] {HTTP};
  % UI STOMP subscriptions
  \draw[arrow,dotted] (tactical) -- (ui)        node[lbl,above] {tactical.decisions};
  % UI submits generation
  \draw[arrow] (ui)       -- (worker)   node[lbl,left] {loop.request};
\end{tikzpicture}
\end{center}

Solid arrows are AMQP~1.0 or STOMP message flows. Dashed arrows are HTTP or
Unix socket calls. The dotted arrow represents the UI's STOMP topic
subscription (\texttt{/topic/tactical.decisions}) that delivers LLM decisions
to the gallery.

% ============================================================
\section{Address and Queue Topology}
% ============================================================

Artemis supports two routing types: \textsc{anycast} (queue semantics, one
consumer receives) and \textsc{multicast} (topic semantics, all subscribers
receive). The table below lists all pipeline addresses.

\begin{center}
\begin{tabular}{lllll}
\toprule
\textbf{Address} & \textbf{Type} & \textbf{Protocol} & \textbf{Publisher} & \textbf{Consumer(s)} \\
\midrule
\texttt{loop.request}          & anycast   & STOMP      & UI server             & comfyui\_worker \\
\texttt{loop.events}           & multicast & STOMP/AMQP & comfyui\_worker       & router, UI server \\
\texttt{scorer.requests}       & multicast & AMQP~1.0   & router                & all scorers \\
\texttt{scorer.events}         & multicast & AMQP~1.0   & aggregator            & all scorers, lancedb\_mgr \\
\texttt{aggregator.clip.queue} & anycast   & STOMP/AMQP & clip\_scorer          & aggregator \\
\texttt{aggregator.artifact.queue} & anycast & STOMP/AMQP & artifact\_scorer   & aggregator \\
\texttt{aggregator.vlm.queue}  & anycast   & STOMP/AMQP & vlm\_scorer           & aggregator \\
\texttt{scorer.result}         & anycast   & AMQP~1.0   & aggregator            & tactical LLM \\
\texttt{loop.accepted}         & multicast & STOMP/AMQP & tactical LLM          & lancedb\_mgr \\
\texttt{tactical.decisions}    & multicast & STOMP      & tactical LLM          & UI server \\
\texttt{pipeline.dead}         & anycast   & AMQP~1.0   & (DLX from all queues) & monitor \\
\bottomrule
\end{tabular}
\end{center}

Python processes use STOMP durable subscriptions to the multicast addresses
\texttt{scorer.requests} and \texttt{scorer.events}. Each scorer connects
with a unique \texttt{client-id} header on \texttt{CONNECT} and subscribes
with a \texttt{durable-subscription-name} header on \texttt{SUBSCRIBE}.
Artemis creates a named durable queue bound to the multicast address,
preserving messages across scorer restarts.

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Process} & \textbf{client-id} & \textbf{Durable subscription names} \\
\midrule
\texttt{clip\_scorer}     & \texttt{clip-scorer}     & \texttt{scorer.clip.requests}, \texttt{scorer.clip.events} \\
\texttt{artifact\_scorer} & \texttt{artifact-scorer} & \texttt{scorer.artifact.requests}, \texttt{scorer.artifact.events} \\
\texttt{vlm\_scorer}      & \texttt{vlm-scorer}      & \texttt{scorer.vlm.requests}, \texttt{scorer.vlm.events} \\
\bottomrule
\end{tabular}
\end{center}

The UI server uses non-durable topic subscriptions (\texttt{client-id:
ui-server}) on \texttt{/topic/loop.events} and
\texttt{/topic/tactical.decisions}. These are broadcast topics; the UI
server receives every message without competing with pipeline consumers.

% ============================================================
\section{Data Flow}
% ============================================================

\begin{enumerate}
  \item The operator opens the browser UI at \texttt{http://localhost:7860}.
        They create a conversation (\texttt{ConversationCreate}) and a
        workflow (\texttt{WorkflowCreate} + \texttt{WorkflowResume}) via the
        UI server, which calls the coordinator over its Unix socket.
        Clicking \textbf{Generate} causes the UI server to publish a
        generation request to \texttt{/queue/loop.request} via STOMP,
        carrying the \texttt{workflow\_id}, \texttt{conversation\_id},
        \texttt{run\_id} (= session\_uuid), and prompt.

  \item \texttt{comfyui\_worker} receives the request, registers the image
        session with the coordinator (\texttt{SessionInit}), submits the
        ComfyUI workflow, and waits for completion via WebSocket.

  \item On completion, publishes the result to \texttt{/topic/loop.events}
        via STOMP. The UI server's STOMP listener receives this message,
        caches the \texttt{image\_uuid $\to$ image\_path} mapping, and
        pushes an \texttt{image\_ready} event over the gallery WebSocket to
        the browser.

  \item Rust \texttt{router} receives from \texttt{loop.events} via AMQP~1.0
        and fans out to \texttt{scorer.requests} (multicast) with subject
        \texttt{score.<uuid>}.

  \item All three scorers receive the message simultaneously and begin scoring
        in parallel (durable STOMP subscriptions).

  \item As results arrive, the Rust \texttt{aggregator} merges them into the
        \texttt{scorer\_session} SQLite row using \texttt{BEGIN IMMEDIATE}
        transactions.

  \item Threshold evaluation on each arrival:
    \begin{itemize}
      \item CLIP score below threshold: mark rejected in SQLite, publish
            \texttt{cancel.*} to \texttt{scorer.events}, emit rejected verdict
            to \texttt{scorer.result}.
      \item Artifact confidence above threshold: same cancel and reject flow.
      \item All three results within bounds: mark candidate in SQLite, emit
            candidate verdict to \texttt{scorer.result}.
    \end{itemize}

  \item \texttt{tactical\_llm} receives the verdict, queries the coordinator
        for budget state (\texttt{BudgetGet}) and user context
        (\texttt{ContextGet} — recent chat messages and thumbs ratings),
        retrieves LanceDB session history, and calls the llama.cpp server's
        OpenAI-compatible API (\texttt{tactical.model.server\_url}) to decide
        accept / retry / inpaint / give\_up. The decision and verdict are
        published to \texttt{/topic/tactical.decisions} via STOMP.

  \item The UI server receives the decision on \texttt{tactical.decisions} and
        pushes a \texttt{decision} event over the gallery WebSocket to the
        browser, which updates the image card's verdict badge.

  \item On accept: tactical LLM publishes to \texttt{/topic/loop.accepted}.
        On retry: publishes a new request to \texttt{/queue/loop.request} and
        increments the retry counter via \texttt{BudgetUpdate}.

  \item \texttt{lancedb\_manager} receives terminal events
        (\texttt{loop.accepted} or \texttt{cancel.*}), reads the completed
        \texttt{scorer\_session} row, performs an idempotent existence check,
        and writes the final Loop record to LanceDB.
\end{enumerate}

\subsection{Cancel Flow}

\begin{center}
\begin{tikzpicture}[node distance=0.7cm]
  \node[component] (clip) {CLIP score};
  \node[component,below=of clip] (clipcheck) {$<$ threshold?};
  \node[component,right=2.2cm of clipcheck] (artifact) {Artifact score};
  \node[component,below=of artifact] (artcheck) {$>$ threshold?};
  \node[component,right=2.2cm of artcheck] (vlm) {VLM score};
  \node[component,below=of vlm] (candidate) {candidate};
  \node[component,left=1.6cm of clipcheck,yshift=-0.8cm] (r1) {rejected};
  \node[component,left=1.6cm of artcheck,yshift=-0.8cm] (r2) {rejected};

  \draw[arrow] (clip) -- (clipcheck);
  \draw[arrow] (clipcheck) -- node[lbl,above] {no} (artifact);
  \draw[arrow] (clipcheck) -- node[lbl,left] {yes} (r1);
  \draw[arrow] (artifact) -- (artcheck);
  \draw[arrow] (artcheck) -- node[lbl,above] {no} (vlm);
  \draw[arrow] (artcheck) -- node[lbl,left] {yes} (r2);
  \draw[arrow] (vlm) -- (candidate);
\end{tikzpicture}
\end{center}

% ============================================================
\section{Persistence Layer}
% ============================================================

\subsection{SQLite Operational Database (\texttt{pipeline.db})}

The coordinator owns \texttt{pipeline.db} in WAL mode with
\texttt{PRAGMA synchronous=FULL}. Seven tables are defined.

\subsubsection{xa\_log}
Durable XA transaction log for 2-phase commit coordination.

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Description} \\
\midrule
\texttt{xid}          & TEXT PRIMARY KEY & Transaction ID \\
\texttt{state}        & TEXT             & prepared $|$ committed $|$ rolledback \\
\texttt{participants} & TEXT             & JSON list of resource managers \\
\texttt{created\_at}  & INTEGER          & Unix timestamp \\
\texttt{resolved\_at} & INTEGER?         & Unix timestamp, null until resolved \\
\bottomrule
\end{tabular}
\end{center}

\subsubsection{scorer\_session}
In-flight per-image state, written by coordinator on \texttt{SessionInit} and
read by aggregator and lancedb\_manager.

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Description} \\
\midrule
\texttt{image\_uuid}      & TEXT PRIMARY KEY & Unique per generated image \\
\texttt{session\_uuid}    & TEXT             & Parent run (= run\_id) \\
\texttt{workflow\_id}     & TEXT?            & Parent workflow \\
\texttt{conversation\_id} & TEXT?            & Parent conversation \\
\texttt{sequence\_number} & INTEGER          & Position within session \\
\texttt{prompt}           & TEXT?            & Generation prompt \\
\texttt{workflow\_path}   & TEXT?            & Workflow file path \\
\texttt{workflow\_params} & TEXT?            & JSON generation params \\
\texttt{clip}             & TEXT?            & JSON scorer result, null until arrived \\
\texttt{artifact}         & TEXT?            & JSON scorer result, null until arrived \\
\texttt{vlm}              & TEXT?            & JSON scorer result, null until arrived \\
\texttt{verdict}          & TEXT?            & null $|$ candidate $|$ rejected \\
\texttt{rejection\_reason}& TEXT?            & Rejection cause, null for candidates \\
\texttt{image\_path}      & TEXT?            & Output image URL \\
\texttt{created\_at}      & INTEGER          & Unix timestamp \\
\texttt{expires\_at}      & INTEGER          & Unix timestamp; row deleted by cleanup \\
\bottomrule
\end{tabular}
\end{center}

\subsubsection{tactical\_budget}
Per-run retry and inpaint budget counters.

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Description} \\
\midrule
\texttt{session\_uuid}    & TEXT PRIMARY KEY & Run identifier \\
\texttt{workflow\_id}     & TEXT?            & Parent workflow \\
\texttt{conversation\_id} & TEXT?            & Parent conversation \\
\texttt{retries\_used}    & INTEGER          & Retry attempts consumed \\
\texttt{inpaints\_used}   & INTEGER          & Inpaint passes consumed \\
\texttt{max\_retries}     & INTEGER          & Run retry ceiling \\
\texttt{max\_inpaints}    & INTEGER          & Run inpaint ceiling \\
\texttt{created\_at}      & INTEGER          & Unix timestamp \\
\texttt{expires\_at}      & INTEGER          & Unix timestamp (24\,h TTL) \\
\bottomrule
\end{tabular}
\end{center}

\subsubsection{conversation}
Top-level grouping of related workflows.

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Description} \\
\midrule
\texttt{id}         & INTEGER PRIMARY KEY & Auto-increment \\
\texttt{uuid}       & TEXT UNIQUE         & External identifier \\
\texttt{name}       & TEXT?               & Human-readable label \\
\texttt{created\_at}& INTEGER             & Unix timestamp \\
\bottomrule
\end{tabular}
\end{center}

\subsubsection{workflow}
A stable creative direction within a conversation. Each \textit{resume}
creates a new run (a new \texttt{tactical\_budget} row); the workflow itself
is long-lived.

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Description} \\
\midrule
\texttt{id}               & INTEGER PRIMARY KEY & Auto-increment \\
\texttt{uuid}             & TEXT UNIQUE         & External identifier \\
\texttt{conversation\_id} & TEXT                & FK to conversation.uuid \\
\texttt{name}             & TEXT?               & Human-readable label \\
\texttt{workflow\_path}   & TEXT                & ComfyUI workflow JSON path \\
\texttt{description}      & TEXT?               & Creative intent \\
\texttt{budget\_policy}   & TEXT                & fresh $|$ carry \\
\texttt{max\_retries}     & INTEGER             & Default retry ceiling \\
\texttt{max\_inpaints}    & INTEGER             & Default inpaint ceiling \\
\texttt{created\_at}      & INTEGER             & Unix timestamp \\
\bottomrule
\end{tabular}
\end{center}

\subsubsection{feedback}
User thumbs up/down ratings recorded by the UI.

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Description} \\
\midrule
\texttt{id}               & INTEGER PRIMARY KEY & Auto-increment \\
\texttt{image\_uuid}      & TEXT                & Image rated \\
\texttt{workflow\_id}     & TEXT                & Parent workflow \\
\texttt{run\_id}          & TEXT                & Parent run \\
\texttt{rating}           & TEXT                & up $|$ down \\
\texttt{comment}          & TEXT?               & Optional text \\
\texttt{created\_at}      & INTEGER             & Unix timestamp \\
\bottomrule
\end{tabular}
\end{center}

\subsubsection{chat\_message}
Chat history between the operator and the tactical LLM, stored per conversation.

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Description} \\
\midrule
\texttt{id}               & INTEGER PRIMARY KEY & Auto-increment \\
\texttt{conversation\_id} & TEXT                & FK to conversation.uuid \\
\texttt{workflow\_id}     & TEXT?               & Optional workflow scope \\
\texttt{role}             & TEXT                & user $|$ assistant \\
\texttt{content}          & TEXT                & Message body \\
\texttt{created\_at}      & INTEGER             & Unix timestamp \\
\bottomrule
\end{tabular}
\end{center}

\subsection{Conversation and Workflow Hierarchy}

The data model is a four-level hierarchy:

\[
\texttt{conversation} \;\to\; \texttt{workflow} \;\to\; \texttt{run (session\_uuid)} \;\to\; \texttt{image\_uuid}
\]

\begin{description}
  \item[Conversation] Persistent across the life of a creative project. Owns
        the chat history. Multiple workflows may exist within one conversation.
  \item[Workflow] A stable creative direction (subject matter, workflow file,
        budget policy). A workflow is \textit{resumed} to start a new run;
        the workflow record itself is never replaced.
  \item[Run] One invocation of the generation loop. Corresponds to a
        \texttt{session\_uuid} in the operational tables. Each run has its
        own \texttt{tactical\_budget} row.
  \item[Image] One generated image within a run. Corresponds to an
        \texttt{image\_uuid} in \texttt{scorer\_session} and LanceDB.
\end{description}

\subsection{Coordinator Unix Socket API}

The coordinator binds a Unix domain socket at
\texttt{\{database.path\}.sock}. Requests and responses are
newline-delimited JSON objects.

\begin{center}
\begin{tabular}{ll}
\toprule
\textbf{Request} & \textbf{Effect} \\
\midrule
\texttt{ConversationCreate} & Insert row into \texttt{conversation}; return \texttt{conversation\_id} \\
\texttt{WorkflowCreate}     & Insert row into \texttt{workflow}; return \texttt{workflow\_id} \\
\texttt{WorkflowResume}     & Insert \texttt{tactical\_budget} for a new run; return \texttt{run\_id} \\
\texttt{BudgetInit}         & Insert row into \texttt{tactical\_budget} (\texttt{INSERT OR IGNORE}) \\
\texttt{BudgetGet}          & Return budget counters for a run \\
\texttt{BudgetUpdate}       & Atomically increment \texttt{retries\_used} or \texttt{inpaints\_used} \\
\texttt{SessionInit}        & Insert row into \texttt{scorer\_session} (\texttt{INSERT OR IGNORE}) \\
\texttt{FeedbackAdd}        & Insert row into \texttt{feedback} \\
\texttt{ChatAdd}            & Insert row into \texttt{chat\_message} \\
\texttt{ContextGet}         & Return recent chat messages and feedback for LLM context \\
\bottomrule
\end{tabular}
\end{center}

Example request/response pairs:

\begin{lstlisting}[style=shell]
# ConversationCreate
{"op":"ConversationCreate","name":"beach portraits"}
{"ok":true,"conversation_id":"conv-uuid-..."}

# WorkflowCreate
{"op":"WorkflowCreate","conversation_id":"conv-uuid-...",
 "workflow_path":"workflows/sdxl_base.json","max_retries":3,"max_inpaints":2}
{"ok":true,"workflow_id":"wf-uuid-...","conversation_id":"conv-uuid-..."}

# WorkflowResume (creates a new run)
{"op":"WorkflowResume","workflow_id":"wf-uuid-..."}
{"ok":true,"run_id":"run-uuid-..."}

# BudgetGet
{"op":"BudgetGet","session_uuid":"run-uuid-..."}
{"ok":true,"retries_used":1,"inpaints_used":0,"max_retries":3,"max_inpaints":2}

# ContextGet
{"op":"ContextGet","conversation_id":"conv-uuid-...","workflow_id":"wf-uuid-...","limit":20}
{"ok":true,"chat":[{"role":"user","content":"more dramatic lighting"},...],
 "feedback":[{"image_uuid":"img-...","rating":"up","comment":"nice"},...]}

# Session not found
{"ok":false,"error":"session not found"}
\end{lstlisting}

\subsection{LanceDB Schema}

\subsubsection{sessions table}
\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Description} \\
\midrule
\texttt{session\_uuid}   & string      & Primary key \\
\texttt{created\_at}     & string      & ISO 8601 \\
\texttt{user\_prompt}    & string      & Original user prompt \\
\texttt{prompt\_embedding} & vector[512] & CLIP text encoding \\
\bottomrule
\end{tabular}
\end{center}

\subsubsection{loop table}
\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Description} \\
\midrule
\texttt{image\_uuid}      & string  & Primary key \\
\texttt{session\_uuid}    & string  & Run identifier \\
\texttt{workflow\_id}     & string? & Parent workflow \\
\texttt{conversation\_id} & string? & Parent conversation \\
\texttt{sequence\_number} & int64   & Position within run \\
\texttt{created\_at}      & string  & ISO 8601 \\
\texttt{prompt}           & string? & Generation prompt \\
\texttt{workflow\_path}   & string? & Workflow file path \\
\texttt{workflow\_params} & string? & JSON generation params \\
\texttt{clip\_score}      & float64? & CLIP similarity \\
\texttt{artifact\_score}  & float64? & AI artifact confidence \\
\texttt{vlm\_photorealism}  & float64? & 0--10 \\
\texttt{vlm\_anatomical}    & float64? & 0--10 \\
\texttt{vlm\_interaction}   & float64? & 0--10 \\
\texttt{vlm\_lighting}      & float64? & 0--10 \\
\texttt{vlm\_prompt}        & float64? & 0--10 \\
\texttt{vlm\_issues}        & string?  & JSON list \\
\texttt{vlm\_recommendations} & string? & JSON list \\
\texttt{verdict}          & string?  & candidate $|$ rejected \\
\texttt{rejection\_reason}& string?  & null for candidates \\
\texttt{image\_path}      & string?  & null for rejected \\
\bottomrule
\end{tabular}
\end{center}

% ============================================================
\section{Browser UI}
\label{sec:ui}
% ============================================================

The pipeline UI is a single-page application served by a FastAPI + uvicorn
server at \texttt{loop/ui/server.py}. It runs in the scorers venv alongside
\texttt{tactical\_llm.py}.

\subsection{REST Endpoints}

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Method} & \textbf{Path} & \textbf{Description} \\
\midrule
\texttt{GET}  & \texttt{/}                           & Single-page application (index.html) \\
\texttt{POST} & \texttt{/session/start}               & Create a conversation \\
\texttt{POST} & \texttt{/workflow/new}                & Create a workflow and first run \\
\texttt{POST} & \texttt{/workflow/\{id\}/resume}      & Resume a workflow (new run) \\
\texttt{POST} & \texttt{/workflow/\{id\}/start-run}   & Enqueue a generation request \\
\texttt{POST} & \texttt{/feedback}                    & Record thumbs up/down for an image \\
\texttt{GET}  & \texttt{/history}                     & Recent chat and feedback for a workflow \\
\texttt{GET}  & \texttt{/image/\{uuid\}}              & Proxy ComfyUI image by UUID \\
\bottomrule
\end{tabular}
\end{center}

\subsection{WebSocket Endpoints}

\begin{description}
  \item[\texttt{/ws/gallery}] Pushes \texttt{image\_ready} and
        \texttt{decision} events to all connected browsers. The server's
        STOMP listener translates Artemis topic messages into gallery events
        broadcast to every open WebSocket connection.
  \item[\texttt{/ws/chat}] Bidirectional streaming chat with the LLM.
        Browser sends \texttt{\{conversation\_id, workflow\_id, message\}};
        server streams back \texttt{\{type:"chunk", text:"...\}$\ldots$\{type:"done"\}}.
        Each exchange is stored via \texttt{ChatAdd} so the tactical LLM
        can retrieve operator intent from \texttt{ContextGet}.
\end{description}

\subsection{Image Path Cache}

Image paths are cached in-process (\texttt{image\_uuid $\to$ URL or local path})
from \texttt{/topic/loop.events} STOMP messages. The \texttt{GET
/image/\{uuid\}} endpoint resolves the path from this cache and either
proxies the ComfyUI HTTP URL or reads the file from disk, depending on
the prefix.

\subsection{Chat Backend Configuration}

By default the chat WebSocket calls the tactical LLM server at
\texttt{tactical.model.server\_url}. Set \texttt{ui.chat\_backend.server\_url}
and \texttt{ui.chat\_backend.model\_name} in \texttt{config.yaml} to switch
the chat panel to a different LLM (e.g., a future strategic model) without
code changes.

% ============================================================
\section{Data-Loss Protection}
% ============================================================

\subsection{XA 2-Phase Commit}

The aggregator uses a 2-phase commit protocol coordinated by the coordinator
binary. SQLite serves as the durable XA log with \texttt{PRAGMA
synchronous=FULL}. Phase~1 (Prepare) writes the participant list and fsyncs
before any phase-2 action. Phase~2 (Commit or Rollback) updates the log
entry and removes the transaction from the in-memory registry.

On startup, the coordinator scans \texttt{xa\_log} for rows in state
\texttt{prepared} and rolls them back (conservative recovery strategy).

\subsection{LanceDB Write Protection}

LanceDB has no XA resource-manager support. Writes are protected by:

\begin{enumerate}
  \item \texttt{scorer\_session} row existence (XA-protected upstream).
  \item Idempotent existence check before each write (\texttt{image\_uuid}
        uniqueness in the \texttt{loop} table).
  \item Message redelivery: ack only after a successful LanceDB write; nack
        without requeue (DLX) on unrecoverable failure.
\end{enumerate}

\subsection{Dead Letter Exchange}

All pipeline queues declare \texttt{x-dead-letter-address: pipeline.dlx}
routing to \texttt{pipeline.dead}. The \texttt{monitor.py} process subscribes
to \texttt{pipeline.dead} and logs all arriving messages for operator review.

% ============================================================
\section{Operations}
% ============================================================

\subsection{Starting the Pipeline}

Ensure \texttt{AI\_IMAGE\_ROOT} is exported in your shell environment (see
Section~2.4). Run the single start script; it manages all startup ordering
internally.

\begin{lstlisting}[style=shell]
$AI_IMAGE_ROOT/loop/start_loop.sh
\end{lstlisting}

\texttt{start\_loop.sh} starts three slow-initialising processes in parallel
(Artemis, ComfyUI, llama.cpp server), waits for them to be ready, then
starts the coordinator, all workers, and the UI server. The full startup
sequence:

\begin{enumerate}
  \item Artemis broker, ComfyUI, and llama.cpp server start in parallel
        (none depend on each other).
  \item \texttt{sleep 10} — covers model loading for ComfyUI and llama.cpp.
  \item Coordinator starts; \texttt{sleep 1} for its Unix socket to bind.
  \item comfyui\_worker, router, aggregator, lancedb\_manager start.
  \item clip\_scorer, artifact\_scorer, vlm\_scorer start.
  \item tactical\_llm starts (connects to llama.cpp server).
  \item UI server starts (FastAPI + uvicorn on \texttt{ui.port}).
  \item monitor starts.
\end{enumerate}

Individual components can also be started manually for development:

\begin{lstlisting}[style=shell]
# Rust binaries
$AI_IMAGE_ROOT/loop/scorers/target/release/coordinator
$AI_IMAGE_ROOT/loop/scorers/target/release/router
$AI_IMAGE_ROOT/loop/scorers/target/release/aggregator
$AI_IMAGE_ROOT/loop/scorers/target/release/lancedb_manager

# ComfyUI
$AI_IMAGE_ROOT/loop/ComfyUI/launch.sh

# llama.cpp OpenAI-compatible server (loads tactical LLM model)
cd $AI_IMAGE_ROOT/loop/scorers
source venv/bin/activate
python -m llama_cpp.server \
  --model models/tactical/Qwen3-72B-abliterated-Q4_K_M.gguf \
  --n_gpu_layers -1 \
  --host 0.0.0.0 \
  --port 8080

# Python workers -- root venv
source $AI_IMAGE_ROOT/venv/bin/activate
PYTHONPATH=$AI_IMAGE_ROOT python $AI_IMAGE_ROOT/loop/comfyui_worker.py

# Python workers -- scorers venv
source $AI_IMAGE_ROOT/loop/scorers/venv/bin/activate
python $AI_IMAGE_ROOT/loop/scorers/clip_scorer.py
python $AI_IMAGE_ROOT/loop/scorers/artifact_scorer.py
python $AI_IMAGE_ROOT/loop/scorers/vlm_scorer.py
PYTHONPATH=$AI_IMAGE_ROOT python $AI_IMAGE_ROOT/tactical-llm/tactical_llm.py

# UI server -- scorers venv
PYTHONPATH=$AI_IMAGE_ROOT python $AI_IMAGE_ROOT/loop/ui/server.py
\end{lstlisting}

\subsection{Using the Browser UI}

Open \texttt{http://localhost:7860} (or the host and port configured in
\texttt{ui.host} / \texttt{ui.port}). The left sidebar lists your
conversations and workflows. Click \textbf{New Session} to create a
conversation, then \textbf{New Workflow} to select a ComfyUI workflow file
and budget parameters. Click \textbf{Generate} to submit a prompt and watch
the gallery fill with images and verdicts in real time. Use the thumbs
up/down buttons on each image card to record feedback; the tactical LLM
reads this via \texttt{ContextGet} when making future decisions. The chat
panel in the right sidebar is a persistent conversation with the LLM for
creative direction.

\subsection{Stopping the Pipeline}

On macOS and Linux:

\begin{lstlisting}[style=shell]
pkill -f comfyui_worker
pkill -f clip_scorer
pkill -f artifact_scorer
pkill -f vlm_scorer
pkill -f tactical_llm
pkill -f "ui/server"
pkill -f "llama_cpp.server"
pkill -f monitor
pkill -f router
pkill -f aggregator
pkill -f coordinator
pkill -f lancedb_manager
"$AI_IMAGE_ROOT/loop/artemis-broker/bin/artemis" stop
\end{lstlisting}

\subsection{Monitoring}

\subsubsection{Artemis management console}

The Artemis web console is available at \texttt{http://localhost:8161}
(default credentials: \texttt{admin} / \texttt{admin}). It provides:

\begin{itemize}
  \item Real-time message counts per address and queue.
  \item Consumer connection status for each durable subscription.
  \item Manual browse, send, and drain operations on any queue.
\end{itemize}

Verify on first start that the following addresses appear under
\textbf{Addresses}: \texttt{loop.request}, \texttt{loop.events},
\texttt{scorer.requests}, \texttt{scorer.result}, \texttt{pipeline.dead}.

\subsubsection{Pipeline UI}

The browser UI at \texttt{http://localhost:7860} shows live image and
decision events as they occur. It is the primary operator interface for
monitoring generation progress, reviewing verdicts, and interacting with
the tactical LLM.

\subsubsection{Dead-letter monitor}

\begin{lstlisting}[style=shell]
source $AI_IMAGE_ROOT/venv/bin/activate
python $AI_IMAGE_ROOT/loop/monitor.py
\end{lstlisting}

Every message appearing in monitor output indicates a processing failure.
The log includes the original queue name (\texttt{\_AMQ\_ORIG\_ADDRESS}
header), message ID, and the full JSON body. Messages remain in
\texttt{pipeline.dead} until explicitly drained or the broker is restarted.

% ============================================================
\section{Memory Budget}
\label{sec:memory}
% ============================================================

The figures below reflect full-GPU-resident operation. On discrete-GPU
systems with insufficient VRAM, the tactical LLM layers can be partially
offloaded to CPU via \texttt{tactical.model.n\_gpu\_layers} in
\texttt{config.yaml} (\texttt{-1} = all layers on GPU).

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Component} & \textbf{Role} & \textbf{Memory} \\
\midrule
ComfyUI + SDXL           & Image generation                     & $\sim$14\,GB \\
comfyui\_worker (Python)  & Loop queue consumer                  & $\sim$50\,MB \\
router (Rust)             & Generation event fanout              & $\sim$5\,MB \\
clip\_scorer (Python)     & Prompt adherence + embedding         & $\sim$1.7\,GB \\
artifact\_scorer (Python) & Photorealism scoring                 & $\sim$500\,MB \\
vlm\_scorer (Python)      & Holistic image evaluation            & $\sim$6\,GB \\
aggregator (Rust)         & Result collection and verdict        & $\sim$5\,MB \\
coordinator (Rust)        & XA coordinator + Python API          & $\sim$5\,MB \\
lancedb\_manager (Rust)   & LanceDB write manager                & $\sim$5\,MB \\
llama.cpp server (Qwen3-72B Q4\_K\_M) & Tactical LLM + chat    & $\sim$43\,GB \\
tactical\_llm (Python)    & Decision logic (model via HTTP)      & $\sim$50\,MB \\
UI server (Python)        & FastAPI + uvicorn + WebSocket        & $\sim$80\,MB \\
ActiveMQ Artemis          & Message broker                       & $\sim$300\,MB \\
OS / system overhead      &                                      & $\sim$2\,GB \\
\midrule
\textbf{Total (GPU)}      &                                      & $\sim$66\,GB \\
\bottomrule
\end{tabular}
\end{center}

The llama.cpp server serves both \texttt{tactical\_llm.py} and the UI chat
endpoint from a single model instance. The VLM scorer and llama.cpp server
do not run simultaneously in the hot path (VLM scoring completes before
tactical inference begins), but both remain resident in GPU memory
throughout the session.

% ============================================================
\section{Threshold Calibration}
% ============================================================

\begin{center}
\begin{tabular}{lll}
\toprule
\textbf{Threshold} & \textbf{Default} & \textbf{Semantics} \\
\midrule
\texttt{thresholds.clip}     & 0.25 & Reject if CLIP score $< 0.25$ \\
\texttt{thresholds.artifact} & 0.50 & Reject if AI confidence $> 0.50$ \\
\bottomrule
\end{tabular}
\end{center}

Both values are placeholders. Calibration procedure:

\begin{enumerate}
  \item Run the loop permissively (\texttt{clip: 0.0}, \texttt{artifact: 1.0}).
  \item Query the \texttt{loop} LanceDB table and examine score distributions.
  \item Set \texttt{clip} at the point below which VLM scores are consistently poor.
  \item Set \texttt{artifact} at the point above which photorealism scores are consistently poor.
  \item Iterate: thresholds too aggressive waste good candidates; too permissive
        waste VLM compute.
\end{enumerate}

All threshold values are read from \texttt{config.yaml} at process startup.
No code changes are required for recalibration.

% ============================================================
\section{Testing}
% ============================================================

\subsection{Rust Tests}

The coordinator test suite covers the XA state machine, crash recovery,
budget operations, \texttt{SessionInit} idempotency, and the full
conversation/workflow hierarchy (ConversationCreate, WorkflowCreate,
WorkflowResume, FeedbackAdd, ChatAdd, ContextGet). Tests run against
an in-memory SQLite database.

\begin{lstlisting}[style=shell]
cd $AI_IMAGE_ROOT/loop/scorers
cargo test -p coordinator

# All crates in the workspace
cargo test
\end{lstlisting}

\subsection{Python Tests}

All Python tests run from the scorers venv:

\begin{lstlisting}[style=shell]
cd $AI_IMAGE_ROOT/loop/scorers
venv/bin/pytest tests/ -v
\end{lstlisting}

Test files and their coverage:

\begin{center}
\begin{tabular}{ll}
\toprule
\textbf{File} & \textbf{Coverage} \\
\midrule
\texttt{test\_clip\_scorer.py}     & CLIP scoring, cancel event handling \\
\texttt{test\_artifact\_scorer.py} & Artifact detection, cancel event handling \\
\texttt{test\_vlm\_scorer.py}      & VLM scoring, cancel event, malformed JSON \\
\texttt{test\_monitor.py}          & Dead-letter formatting, listener ack/count \\
\texttt{test\_ui\_server.py}       & REST endpoints, WebSocket events, image proxy,
                                     gallery manager, STOMP listener \\
\bottomrule
\end{tabular}
\end{center}

External dependencies (LanceDB, Unix socket, STOMP broker, llama.cpp server)
are mocked in all tests. The UI server tests inject a fake config module so
they run without \texttt{config.yaml}.

\subsection{Full Suite}

\begin{lstlisting}[style=shell]
cd $AI_IMAGE_ROOT
make all    # lint + typecheck + test + build
\end{lstlisting}

% ============================================================
\section{Scale-Out Path}
% ============================================================

\begin{description}
  \item[Multiple ComfyUI workers] Add STOMP consumers on \texttt{/queue/loop.request}.
  \item[Multiple scorer instances] Durable STOMP subscriptions ensure each
        process gets an independent copy of each score request. Add processes;
        no broker reconfiguration needed.
  \item[Remote scoring nodes] Change \texttt{broker.stomp\_url} in
        \texttt{config.yaml}. Artemis handles clustering natively.
  \item[Managed broker] Migrate to Amazon MQ for ActiveMQ (AMQP~1.0 / STOMP
        compatible).
  \item[Cloud LanceDB] Change \texttt{paths.lancedb} to an S3 URI.
  \item[Strategic LLM for chat] Set \texttt{ui.chat\_backend.server\_url} and
        \texttt{ui.chat\_backend.model\_name} in \texttt{config.yaml}. No code
        changes required.
\end{description}

\end{document}
